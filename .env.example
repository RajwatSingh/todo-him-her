# Copy this file to `.env` and fill in the two values from your Supabase
# project: Dashboard → Project Settings → API.
#
# Until both are set the app runs happily in local-only mode (everything is
# kept in this browser's localStorage and nothing syncs).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
